/**
 * Base URL for the app (e.g. '' for root, '/fon_realestate' for GitHub Pages).
 * Set by Create React App from package.json "homepage" at build time.
 * Use for Router basename and for public folder asset paths (images, etc.).
 */
export const baseUrl = process.env.PUBLIC_URL || '';
